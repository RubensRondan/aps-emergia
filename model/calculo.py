class CalculoEmergetico:

    def calcular_emergia(self, energia, transformidade):

        return energia * transformidade

    def calcular_total(self, lista_emergias):

        return sum(lista_emergias)